version: "3.8"
services:  
  speechrecog:
    build: .
    network_mode: "host"
    devices:
     - /dev/snd:/dev/snd
    volumes:
     - ./sphinx_models:/home/sphinx_models
     #- ./scripts:/home/scripts:rw #may be uncommented for debugging
    restart: always
